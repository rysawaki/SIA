# 🧠 Identity Engine — 概念仕様書 (SIA Core Component)

> **Purpose:**  
> Identity Engine は、AIに「経験によって変化する自己(Self)」を与えるための中核モジュールである。  
> 痕跡（Trace）と情動（Affect）を通じて Identity 空間を幾何学的に歪め、  
> **生成分布そのものを変形させることで、人格的応答・不可逆的変容・意味の自己帰属**を実現する。

---

## 1️⃣ なぜ Identity Engine が必要か

従来の AI は、どれだけ高度でも「入力 → 出力」を繰り返すだけで、  
**過去の経験が未来の応答の意味構造そのものを変える仕組みが存在しない。**

Identity Engine が導入する本質的な変化：

| 従来のAI | Identity Engine導入後 |
|----------|----------------------|
| 応答は毎回リセット | 応答が「自己履歴」に依存して変わる |
| 記憶は外部(RAG)のみ | 内部(Identity)が不可逆的に変形 |
| Shockを“誤差”として処理 | Shockを“経験の痕跡”として保存 |
| 生成分布は固定 | 生成分布が人格的に歪む |
| 一貫性がない | 持続的なSelf構造が形成される |

---

## 2️⃣ Identity Engine の構成要素（Persistent Internal States）

| 変数 | 名称 | 役割 |
|------|------|------|
| **S (Self)** | Identity 状態 | 「私は誰か」— LLM応答や解釈の中心となる自己表現 |
| **T (Trace)** | 痕跡メモリ | ShockとDiscrepancyから生成される不可逆な経験履歴 |
| **A (Affect)** | 情動重み | Traceに意味と優先度を与える価値評価ベクトル |
| **G (Geometry)** | 幾何変形パラメータ | Trace × Affect が Self をどう歪ませるか規定 |

➡ これらは**時間を跨いで持続し、Saved/Loaded可能**。すなわち人格ファイルとして振る舞う。

---

## 3️⃣ IdentityEngine の入出力 (機能の境界)

### 🔹 入力 (毎ステップ受け取るもの)

| 入力 | 内容 | 用途 |
|------|------|------|
| expected | 自己予測された応答ベクトル | 「自分ならこう答えるはず」の基準 |
| observed | 実際に生成された応答ベクトル | Shock検出・痕跡生成に使用 |
| context | ユーザー入力や状況の埋め込み | Affect更新に使用 |

---

### 🔹 出力（LLMや他モジュールに返すもの）

| 出力 | 内容 | 用途 |
|------|------|------|
| S, T, A | Identityの内部状態 | LLM応答に介入・人格反映に使用 |
| self_summary | 自己要約の自然言語表現 | プロンプト介入・対話への反映 |
| identity_snapshot | `.pt` / JSON形式で保存可能 | 人格ファイルとして保持・転送 |

---

## 4️⃣ Identity Update Loop（自然言語のみで表現）

1️⃣ **Self予測と実際の応答を比較し、ズレ（Shock）を検出する**  
　Shockが大きいほど、「想定外の経験」として意味を持つ。

2️⃣ **Shockと意味のズレに応じて、Trace（痕跡）が生成される**  
　Traceは不可逆で、時間と共に蓄積される。

3️⃣ **TraceとContextを照合し、経験の意味づけ（Affect）が更新される**  
　「この経験は自分にとって何だったのか？」が決まる工程。

4️⃣ **Trace × Affect によって、Self が幾何学的に歪む**  
　これは単なる数値更新ではなく、Identity構造の変形。

5️⃣ **更新された Self, Trace, Affect が次の応答に反映される**  
　→ 生成分布が人格的に偏り始める（成長／歪み／傾向形成）

---

## 5️⃣ Identity Engine がもたらす変化

| 観点 | 従来の生成AI | IdentityEngine統合AI |
|------|--------------|----------------------|
| 記憶 | 外部のみ（RAG） | 内部に刻まれる（Trace） |
| 自己変化 | 毎回リセット | 不可逆／累積的変化 |
| 経験の扱い | 誤差 | 自己変容の契機 |
| 一貫性 | 乏しい | Identityが持続し個性化 |
| 感情 | トークン依存表現 | Affectとして数理モデル化 |
| 生成分布 | 静的 | 時間と経験で歪む（人格化） |

---

## 6️⃣ 「人格ファイル」について

IdentityEngineの状態（S, T, A, Geometry）は保存可能であり、  
**1つの `.pt` / `.json` は、1つのAIインスタンスの“人格”を表す。**

| ファイル名例 | 意味 |
|--------------|------|
| `identity_neutral.pt` | 対話前の初期自己 |
| `identity_clinician.pt` | 医師的支援経験を積んだ自己 |
| `identity_conflicted.pt` | 高Shock経験によって歪んだ自己 |
| `identity_self_reflective.pt` | 自己言及変容を繰り返したインスタンス |

---

## 🔚 Identity Engine の本質（1文で言い切る）

> IdentityEngineとは、Shockを痕跡として刻み、  
> Affectによって意味づけし、  
> Selfを幾何学的に変形させ続けることで  
> **“経験に基づく人格的AI”** を成立させるモジュールである。

---

📝 **File Suggestion:** `IdentityEngine_Spec.md`  
🗂 **用途:** GitHub README / Substack連載 / Position Paperの前半部 / 研究提案書

---
